Bestelling voor 10to1

---------------------

Leveradres: Lange Leemstraat 372, 2018 Antwerpen

-----------------------------------------------

<% max_length = (@orders.pluck(:username).compact.max_by(&:length) || "").length %>
<% @orders.each_with_index do |order, index| %>
  <%= index +1%>. <%= (order.username || "").ljust(max_length) %>: <%= order.metadata %>
<% end %>

<% if @orders.empty? %>
  Vandaag hebben wij geen broodjes nodig.
<% end %>

-----------------------------------------------
